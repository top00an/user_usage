{
  "_comment": "config.json 으로 복사해 쓴다. usage 블록만 읽으며, 없거나 깨져도 서비스는 시드 단가로 정상 동작한다(throw 하지 않는다).",

  "usage": {
    "_pricedAt": "이 단가표가 언제 것인지. 화면이 그대로 표시해 낡은 값이 보이게 한다.",
    "pricedAt": "2026-08-13",

    "_pricing": "USD / 1M 토큰. 여기 적은 모델만 시드를 덮어쓴다(부분 오버라이드). input·output 이 둘 다 있어야 인정된다.",
    "_pricing_warning": "이 블록은 **시드를 이긴다.** 그래서 예시를 그대로 복사하면 시드가 나중에 고쳐져도 여기 적힌 낡은 값이 계속 이긴다. 실제로 그 사고가 났다 — 예시의 claude-sonnet-5 가 $3/$15 였는데(취소된 인상 예고를 적어 둔 값) 실제 표준가는 $2/$10 이라, 이 파일을 복사한 배포는 1.5배 과대계상됐다. **사내 게이트웨이 모델처럼 시드가 모르는 단가를 꽂을 때만 쓰고, 시드에 이미 있는 모델은 적지 마라.**",
    "pricing": {
      "internal-gateway-model": { "input": 10, "output": 30 }
    },

    "_mult": "캐시 축의 입력가 대비 배수. 캐시읽기 0.1배, 캐시생성은 5분 TTL 1.25배·1시간 TTL 2배가 기본이다.",
    "cacheReadMult": 0.1,
    "cacheCreateMult": 1.25,
    "cacheCreate1hMult": 2
  }
}
