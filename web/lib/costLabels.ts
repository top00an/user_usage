/*
 * ── 비용 라벨의 단일 출처 ─────────────────────────────────────────────────
 *
 * 화면이 "비용"이라고만 쓰면 사람은 그것을 **청구서 금액**으로 읽는다. 우리 숫자는 그게 아니다.
 *
 * 팀은 구독 요금제(Claude Max·Team, ChatGPT Plus·Pro)로 쓴다. 구독은 토큰당 과금이 아니므로
 * 실제 청구액은 **정액**이고, 사용량이 늘어도 줄어도 그대로다. 우리가 계산하는 값은
 * "같은 사용량을 공개 API 종량제로 돌렸다면 얼마였을까"의 **환산 추정치**다.
 *
 * 그래서 라벨을 'API 환산 비용'으로 고정한다. 쓸모는 그대로다 — 사용자·모델·플랫폼 간
 * **상대 비교**와 "어느 축이 비싼가"를 보는 자다. 다만 그걸 청구액이라고 부르지 않는다.
 *
 * 문구를 여기 모으는 이유: 대시보드·플랫폼 요약·좌석·팀·리더보드·세션이 각자 다른 말로 쓰면
 * 같은 숫자가 화면마다 다른 것으로 읽힌다. 그 상태가 이 파일 이전의 상태였다
 * (Total Cost · 비용 · 환산액 · API 환산액이 뒤섞여 있었다).
 */

/** 표준 라벨. 카드 제목·표 헤더의 기본형. */
export const COST_LABEL = 'API 환산 비용';

/** 표 열처럼 폭이 좁은 자리의 축약형. 같은 것을 가리킨다. */
export const COST_LABEL_SHORT = '환산 비용';

/** 한 줄 각주 — 라벨 옆·툴팁에 그대로 붙인다. */
export const COST_DISCLAIMER = '구독 요금제 사용 시 실제 청구액과 다릅니다 · 공개 API 단가 기준';

/** 툴팁·각주의 긴 설명. 왜 다른지까지 말한다. */
export const COST_WHY =
  '구독 요금제(Claude Max·Team, ChatGPT Plus·Pro)는 토큰당 과금이 아니라 정액이므로,'
  + ' 이 숫자는 실제 청구액이 아닙니다. 같은 사용량을 공개 API 단가로 종량 과금했다면 나왔을'
  + ' 금액의 환산 추정치이며, 사용자·모델·플랫폼 간 상대 비교와 비용이 큰 축을 찾는 용도입니다.';
